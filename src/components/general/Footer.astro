---
import { buttonVariants } from "@/components/ui/button";
import { info } from "@/data/info";
import { Icon } from "astro-icon/components";

const iconSize = 25;
---

<footer
  class="container mt-16 mb-4 flex flex-col-reverse items-center justify-between gap-8 sm:flex-row md:mb-8 md:flex-row"
  aria-label="Footer"
>
  <div class="flex h-5 items-center space-x-6 text-sm">
    <a class={buttonVariants({ variant: "link", size: "none" })} href="/imprint">Impressum</a>
    <a class={buttonVariants({ variant: "link", size: "none" })} href="/privacy">Datenschutz</a>
  </div>
  <div class="grid grid-cols-3 items-center gap-4">
    <a
      aria-label="GitHub"
      href={info.socialMedia.github}
      class={buttonVariants({ variant: "ghost", size: "icon-sm" })}
      target="_blank"
      ><Icon name="github" size={iconSize} aria-label="GitHub Icon" />
    </a>
    <a
      aria-label="LinkedIn"
      href={info.socialMedia.linkedin}
      class={buttonVariants({ variant: "ghost", size: "icon-sm" })}
      target="_blank"
    >
      <Icon name="linkedin" size={iconSize} aria-label="LinkedIn Icon" />
    </a>
    <a
      aria-label="E-Mail"
      href={`mailto:${info.socialMedia.email}`}
      class={buttonVariants({ variant: "ghost", size: "icon-sm" })}
      target="_blank"
    >
      <Icon name="email" size={iconSize} aria-label="Email Icon" />
    </a>
  </div>
</footer>

---
import EmailIcon from "@/assets/icons/email.svg";
import GithubIcon from "@/assets/icons/github.svg";
import LinkedinIcon from "@/assets/icons/linkedin.svg";
import Button from "@/components/ui/Button.astro";
import { info } from "@/data/info";
import { ICON_SIZE } from "@/utils/constants";
---

<footer
  data-slot="container"
  class="mt-16 mb-4 flex flex-col-reverse items-center justify-between gap-8 sm:flex-row md:mb-8 md:flex-row"
  aria-label="Footer"
>
  <div class="flex h-5 items-center space-x-6 text-sm">
    <Button variant="link" size="none" href="/imprint">Impressum</Button>
    <Button variant="link" size="none" href="/privacy">Datenschutz</Button>
  </div>
  <div class="grid grid-cols-3 items-center gap-4">
    <Button variant="ghost" size="icon-sm" href={info.socialMedia.github} aria-label="GitHub Profil">
      <GithubIcon width={ICON_SIZE} height={ICON_SIZE} aria-hidden="true" />
    </Button>
    <Button variant="ghost" size="icon-sm" href={info.socialMedia.linkedin} aria-label="LinkedIn Profil">
      <LinkedinIcon width={ICON_SIZE} height={ICON_SIZE} aria-hidden="true" />
    </Button>
    <Button variant="ghost" size="icon-sm" href={`mailto:${info.socialMedia.email}`} aria-label="E-Mail senden">
      <EmailIcon width={ICON_SIZE} height={ICON_SIZE} aria-hidden="true" />
    </Button>
  </div>
</footer>

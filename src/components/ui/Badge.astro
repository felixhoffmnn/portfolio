---
interface Props {
  variant?: "primary" | "secondary" | "destructive" | "outline";
}

const { variant = "primary" } = Astro.props;
---

<div data-slot="badge" data-variant={variant}>
  <slot />
</div>

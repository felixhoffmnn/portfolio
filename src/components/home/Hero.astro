---
import { IHeroProps } from "@types";

const { name, about } = Astro.props as IHeroProps;
---

<section>
  <p class="dark:text-light mt-[117px]">Hey, ich bin</p>
  <h1>{name}</h1>
  {about.map((paragraph) => <p class="dark:text-light text-lg mb-2 last:mb-16">{paragraph}</p>)}
</section>
